# Local semantic neural Actor integration

The first learning gate is restricted to main-action option kinds present in its qualified teacher data. Effect targeting, search/assignment and other untrained interactions use the explicit `unsupported_learning_context` rule lane, even when a parent prompt is labeled `main`. This gate was corrected from actual model-visited development traces; the original runtime/result is retained rather than relabeled.

2026-09-19. Authorized development integration for the exact 646600 Marnie deck in Strategy Forge. No release, production installation or private service changes.

`ptcgdap_local_semantic_actor_i32_v1` adds frame 128 / option 32 inputs, up to 1024 options, with explicit presence masks and sealed-deck categorical identity. The old 24/16 profile remains independently recognized. The Python and Godot projectors use only the current allow-listed public frame, this seat's visible hand and public option semantics. Unknown own UIDs and unsupported semantic shapes bypass the Actor through a diagnosed rule path.

Competitive Base returns `model_frontier` outside its existing audit hash. The frontier binds the current observation/window and contains only same-best-tier, non-vetoed options. Mandatory/terminal windows, a public final-prize attack, quota/cardinality requirements, bound routes/transactions, rule fallback and unique choices remain rule-owned. The worker and synchronous development owner pass this frontier to `PtcgDAPModelActor`; a model never gains engine access or durable index authority.

`SemanticModelInput.gd` and `scripts/ai/ptcgdap/semantic_model_profile.py` share the versioned projection. Developer traces capture the exact profile/projector hash, frontier, tensors and row binding. These fields are diagnostic evidence, not independent execution authority. Training admission additionally requires trace integrity, teacher/Host agreement and a single-decision engine-commit witness.

The native C++ Actor recognizes only the two reviewed fixed shape profiles and uses the selected dimensions for warmup and inference. Windows v4 is built with the existing CPU-only ORT runtime and 25 ms budget; old v3 binaries remain for rollback. Android and macOS builds have not been rebuilt or accepted for this new profile. The official raw-CABT input lane explicitly bypasses this local-development profile.

Forge evidence in `work/marnies-gift-box-neural-20260918/` includes the approved interface proposal, rollback bytes, 135 real-window cross-language projection cases, 232 native Actor cases (including reorders and rule gates), exact double-build/scenario results and trained BC checkpoints. Final full-game results and limitations are in that workspace's `RESULTS.md`; native projection conformance alone does not prove playing strength.

Rollback the model to the exact frozen rules package and retain a compatible runtime. Do not restore the whole dirty worktree: task-specific rollback snapshots cover only the touched pre-change files. Test-fixture signatures remain development-only; CABT engine parity, other devices and production are independent gates.
