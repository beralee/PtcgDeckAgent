# AI天梯排名展示（2026-09-15）

AI 策略中心的发现入口统一命名为 `AI天梯`。连续天梯策略卡与快照策略排名使用独立的大字号排名行；前三名分别采用金、银、铜色边框与背景，榜首标记为 `#1 · 榜首`。连续天梯作者榜复用相同的排名装饰。手机卡片保留策略名、作者、积分、局数和暂定状态。

排名完全沿用接口返回的 `rank`，不按页面位置重新编号，不改积分或排序规则。分页第 24 名不会变成榜首，未知排名显示“未排名”，最新上架列表不授予排名标识。原有下载、安装、开战与策略详情事件保持原绑定。装饰文字忽略输入，滚动继续交给所属 ScrollContainer。

## 验证

- 新增 `test_strategy_hub_ladder_presentation.gd`：最初 5 项均失败；实现与补充交互测试后 6 项通过。单行标签宽度检查也先复现失败，再通过窄屏内边距修正。
- 相关回归：StrategyHub 场景 22 项、桌面下载开战流程 4 项、响应布局 4 项、输入兼容性 10 项通过。合计 46 项。
- 实际 Godot 渲染检查：390×844 手机和 1600×900 桌面，长中文策略名、前三名样式、作者与积分均可见。图片位于 `.tmp/ai-ladder/ladder-390.png` 与 `ladder-1600.png`，使用测试数据。
- Android/Web 手机尺寸的真实 SubViewport 输入派发测试覆盖从排名文字和策略按钮起拖、滚动时不误点、轻点仅激活一次。此项是输入与布局模拟，不等同于此次进行实体手机或浏览器实测。
- 新用例已加入 `scripts/tools/run_ui_compatibility.ps1`，日志保存在 `.tmp/ai-ladder/`。

## 范围与回滚

本次仅修改客户端展示与对应测试，没有修改私有服务、榜单计算、策略执行或数据合同。回滚仅移除 StrategyHub 的排名装饰、入口文案与窄屏标签内边距，以及新增测试和测试入口；保留这些文件中原有的其他改动。当前证据属于 UI 展示与交互回归，不新增策略或引擎对齐声明。
